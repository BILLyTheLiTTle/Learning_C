#include <stdio.h>
#include "header_1.h" // Needs to be included no matter how I run it (F5 from Atom or g++ from command line)!

int main(int argc, char const *argv[]) {
  printf("%d\n", ONE);
  return 0;
}
